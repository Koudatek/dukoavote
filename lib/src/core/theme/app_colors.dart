import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales inspirées de l'image
  static const Color primary = Color(0xFF8B5CF6); // Violet doux
  static const Color primaryLight = Color(0xFFA78BFA); // Violet plus clair
  static const Color primaryDark = Color(0xFF7C3AED); // Violet plus foncé
  
  // Couleurs de fond
  static const Color background = Color(0xFFF8FAFC); // Blanc cassé très doux
  static const Color surface = Color(0xFFFFFFFF); // Blanc pur
  static const Color surfaceVariant = Color(0xFFF1F5F9); // Gris très clair
  
  // Couleurs de texte
  static const Color textPrimary = Color(0xFF1E293B); // Gris foncé élégant
  static const Color textSecondary = Color(0xFF64748B); // Gris moyen
  static const Color textTertiary = Color(0xFF94A3B8); // Gris clair
  
  // Couleurs d'accent
  static const Color accent = Color(0xFF06B6D4); // Cyan doux
  static const Color success = Color(0xFF10B981); // Vert doux
  static const Color warning = Color(0xFFF59E0B); // Orange doux
  static const Color error = Color(0xFFEF4444); // Rouge doux
  
  // Couleurs utilitaires
  static const Color divider = Color(0xFFE2E8F0);
  static const Color shadow = Color(0xFF1E293B);
  static const Color overlay = Color(0x80000000);
  
  // Couleurs pour l'onboarding
  static const Color onboardingBackground = Color(0xFFF1F5F9);
  static const Color onboardingCard = Color(0xFFFFFFFF);
  static const Color onboardingText = Color(0xFF1E293B);
  static const Color onboardingSubtext = Color(0xFF64748B);
}